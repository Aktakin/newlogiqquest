{
  "buildCommand": "npm run build",
  "outputDirectory": "packages/client/dist",
  "installCommand": "npm install --include=dev --foreground-scripts --ignore-scripts=false",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
